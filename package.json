{
  "name": "dye-room-humidity-report",
  "version": "1.0.0",
  "description": "Humidity/temperature dashboard for a dye storage room (Netlify + Neon)",
  "type": "module",
  "scripts": {
    "migrate": "node migrate.js",
    "seed": "node seed.js",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": { "node": ">=18" }
}
